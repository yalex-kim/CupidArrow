import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import ArrowDodgeGame from './components/ArrowAvoidGame'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ArrowDodgeGame />
  </React.StrictMode>,
)